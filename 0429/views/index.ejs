<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Document</title>
</head>
<body>
    <h1>index 페이지 접속 성공</h1>
    서버에서 보내준 num 값: <%= num %>

<% if (num % 2 == 0) { %>
    <h1>짝수입니다</h1>
<% } else { %>
    <h1>홀수입니다</h1>
<% } %>

    <table border="1">
        <tr>
            <% for (let i = 1; i <= num; i++) { %>
            <td><%= i %></td>
            <% } %>
        </tr>
    </table>
</body>
</html>